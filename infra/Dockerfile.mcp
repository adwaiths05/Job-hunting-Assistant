FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (including express/cors for the server)
RUN npm install && npm install express cors

# Copy the source code
COPY . .

# Expose the port (we will define this in docker-compose)
EXPOSE 8080

# Start the server
CMD ["node", "index.js"]